Write a Python program that determines if a given year is a leap year. A year is a leap year if:

It is divisible by 4, and
If it is divisible by 100, it should also be divisible by 400.
The program should print "Leap Year" if the year is a leap year, otherwise print "Not a Leap Year".
